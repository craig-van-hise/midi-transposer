# Product Requirements Prompt (PRP) — ENGINE ACTIVATION HOTFIX ✅ Completed

## 1. Context & Objective
**STATUS: CRITICAL ENGINE OFFLINE**
The `useWebMidi` custom hook is perfectly implemented, but the application is currently deaf to hardware because the hook is never actually invoked in the component tree. During the Phase 5 cleanup, the hook invocation was omitted from `App.tsx`. The objective is to mount the hook at the root level so `requestMIDIAccess` fires on load.

## 2. Mandatory Execution Constraints
* **DO NOT** edit `useWebMidi.ts`, `useMidiStore.ts`, or `Header.tsx`. They are correct.
* Target `src/App.tsx` ONLY.

## 3. Implementation Phases

### Phase 1: Mount the Web MIDI Hook — [Status Marker] ✅ Completed
* **Objective:** Execute the hook inside the main App component.
* **Instructions:**
  1. Open `src/App.tsx`.
  2. Add the import for the hook at the top of the file: 
     `import { useWebMidi } from './hooks/useWebMidi';`
  3. Inside the main `App` component function, at the very top (before the `return` statement), invoke the hook:
     `useWebMidi();`
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Edit `App.tsx` to import and call the hook. Once applied, set status to ✅ Completed."

## 4. Final Review
* **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to refresh the page. Verify: 1. The browser requests MIDI permissions (if not already granted). 2. The Header dropdown successfully populates with the connected MIDI devices. 3. The pipeline routing test from the previous PRP now works (left hand transposes, right hand plays). Do not proceed until the user types 'VALIDATED'."