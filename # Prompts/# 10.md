# Product Requirements Prompt (PRP) — CRITICAL HEIGHT & MARGIN FIX 🔘

## 1. Context & Objective
**STATUS: EMERGENCY LAYOUT OVERRIDE**
The previous layout execution failed. The margins between cards expanded instead of contracting, and the Output card is vertically bloated because the "FILTER MODE" text stacked under the "Output" title. 
**Goal:** Group the filter text and buttons inline, center them together over C4, crush the vertical padding to match the Input card's height, and strictly enforce a small gap between cards.

## 2. Mandatory Execution Constraints
* **DO NOT** edit any `.md` files. 
* Target `.tsx` files ONLY.

## 3. Implementation Phases

### Phase 8.1: Output Card (NoteFilter) Inline Grouping & Compacting — [Status Marker] 🔘
* **Objective:** Un-stack the header to crush the card height, and center the text+buttons as one unit over C4.
* **Instructions:**
  1. Open `src/components/keyboards/NoteRangeFilterKeyboard.tsx`.
  2. Locate the top header area. Nuke its current layout and replace it with this exact structure:
     * A root header `div` with: `relative w-full h-[32px] flex items-center`.
     * The "Output" title (and its chevron) positioned on the far left.
     * **The Grouped Container:** Create a single `div` that contains BOTH the "FILTER MODE" text and the map of toggle buttons. Give this grouped container: `absolute left-[446px] -translate-x-1/2 flex items-center gap-4`. (This places the text inline to the left of the buttons, and centers the whole block over Middle C).
  3. **Crush Vertical Padding:** * On the main card root `div`, ensure top and bottom padding are minimal (e.g., `pt-4 pb-4`).
     * Remove ALL `mt-*`, `mb-*`, `py-*`, and `my-*` classes from the slider container and the header container so the elements sit as tightly together as possible without overlapping.

### Phase 8.2: Strict App.tsx Canvas Margins — [Status Marker] 🔘
* **Objective:** Force a tight, uniform gap between the title bar and all cards.
* **Instructions:**
  1. Open `src/App.tsx`.
  2. Locate the flex column holding the three keyboards.
  3. Strip any rogue margin classes (`mt-*`, `mb-*`) from the components within this file.
  4. Force the container gap to be exactly `gap-4` (16px). 
  5. Ensure the padding below the Header matches this exact gap size (`pt-4`).

* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Edit the `.tsx` files exactly as stated to un-stack the Output header and tighten the canvas gaps. Once applied, set status to ✅ Completed."

## 4. Final Review
* **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to refresh. Verify: 1. The Output card is now identical in height to the Input card. 2. The 'FILTER MODE' text is side-by-side with the buttons, not stacked. 3. The gaps between all cards are tight and uniform. Do not proceed until the user types 'VALIDATED'."