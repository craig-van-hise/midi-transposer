# Product Requirements Prompt (PRP) — 🔘 Pending

## 1. Project Context & Objectives
**Context:** The previous UI refactor introduced naming regressions and incorrectly reordered an existing UI array. 
**Objective:** 1. Rename all instances of "Hold Mode" to "Sustain Mode" across the UI and Zustand store.
2. Update the Polyphony Tooltip to explicitly communicate the Play zone monophonic restriction.
3. Restore the exact original rendering order of the Output Filter modes.

## 2. Technical Decisions
* This is a strict surgical refactor. Do not alter any underlying MIDI routing logic. Only target strings, state variable names, and array map orders.

---

## 3. Implementation Phases

### Phase 1: Terminology & State Renaming — ✅ Completed
* **Objective:** Enforce "Sustain Mode" naming and update tooltips.
* **Instructions:**
    1. Open `src/store/useMidiStore.ts`.
    2. Rename the type `TransposeHoldMode` to `TransposeSustainMode`.
    3. Rename the state variable `transposeHoldMode` to `transposeSustainMode` and update its setter `setTransposeSustainMode`.
    4. Open `src/hooks/useWebMidi.ts` and update all variable references from `transposeHoldMode` to `transposeSustainMode` within the `useEffect` subscription.
    5. Open `src/components/keyboards/TransposeKeyboard88.tsx`.
    6. Change the modal section title from "Transpose Hold Mode" to "Transpose Sustain Mode".
    7. Update the tooltip text for the "Poly" option to explicitly read: *"Allows parallel chord transpositions. Enforces strict monophonic (last-note priority) playing in the Play zone."*
* **TDD Checkpoint:**
    * **Test Case 1:** Zustand store initializes and successfully updates `transposeSustainMode`.
    * **Test Case 2:** `TransposeKeyboard88` renders without TypeScript errors after the prop renaming.
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Execute tests. Once PASS, set status to ✅ Completed.
 
### Phase 2: Filter Mode Order Restoration — 🔵 Active
* **Objective:** Revert the Output filter radio buttons to their original, required sequence.
* **Instructions:**
    1. Open `src/components/keyboards/NoteRangeFilterKeyboard.tsx`.
    2. Locate the array or rendering block used to map the Filter Mode radio buttons inside the new Modal.
    3. Strictly enforce this exact rendering order: `['block', 'octave_wrap', 'wrap', 'limit']`.
    4. Update the corresponding UI labels to render in this exact sequence from left-to-right (or top-to-bottom, depending on the modal layout): "Block", "Octave Wrap", "Wrap", "Limit".
* **HUMAN VALIDATION REQUIRED:** * **AGENT INSTRUCTION:** "STOP. Set Phase status to 🔵 Active. Ask the user to run the app, open both settings modals, and verify: 1) The title says 'Sustain Mode', 2) The Poly tooltip mentions the Play zone restriction, and 3) The Output Filter order is explicitly Block -> Octave Wrap -> Wrap -> Limit. Wait for user confirmation before marking as ✅ Completed."

---

## 4. Final Review & Validation
* **Technical Audit:** Run a global TypeScript compilation check (`tsc --noEmit`) to ensure no lingering references to `transposeHoldMode` exist anywhere in the codebase.
* **Status Reconciliation:** Verify all sub-phases are marked ✅. Update the top-level PRP status to ✅ Completed.