# Product Requirements Prompt (PRP) — KEYBOARD DOM CLONE & GLITCH FIX 🔘

## 1. Context & Objective
**STATUS: SURGICAL UI CLONING**
The `NoteRangeFilterKeyboard` (Output) currently has a 1px-2px white gap/border around its keys due to rogue padding, making it look inconsistent with the Input and Transpose keyboards. The objective is to rip out the flawed keyboard DOM in the Output component and replace it with the exact, pristine HTML/CSS structure used in `TransposeKeyboard88`, while retaining the Output component's specific out-of-range dimming logic.

## 2. Mandatory Execution Constraints
* **DO NOT** edit any `.md` files. 
* Target `.tsx` files ONLY.

## 3. Implementation Phases

### Phase 9.1: Clone Key Structure into NoteRangeFilterKeyboard — [Status Marker] 🔘
* **Objective:** Eradicate the white border glitch by unifying the key rendering logic.
* **Instructions:**
  1. Open `src/components/keyboards/NoteRangeFilterKeyboard.tsx`.
  2. Locate the `Piano88Filter` component and its `piano-container-88` wrapper `div`.
  3. **Strip Rogue Padding:** Remove `p-[2px]` or any `padding` applied to the `piano-container-88` wrapper. It must have zero internal padding, matching the `#keyboard-wrapper` in `TransposeKeyboard88.tsx`.
  4. **Clone White Key Styles:** Look at the `style` object applied to the white keys in `TransposeKeyboard88.tsx`. Copy those exact properties into the white key `div` inside `NoteRangeFilterKeyboard.tsx`. Ensure `borderLeft`, `borderRight`, and `borderBottom` are explicitly `'1px solid #7a7a7a'` and `borderTop` is `'none'`.
  5. **Clone Black Key Styles:** Look at the `style` object applied to the black keys in `TransposeKeyboard88.tsx`. Copy those exact 3D properties into the black key `div` inside `NoteRangeFilterKeyboard.tsx`.
  6. **Preserve Dimming Overlay:** After updating the styles of the keys to match the clone, ensure that the absolute-positioned dimming `div`s (e.g., `<div style={{ position: 'absolute', inset: 0, backgroundColor: 'rgba(0,0,0,0.15)'...`) are still rendered inside the keys when `isOutOfRange` is true.

* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Edit `NoteRangeFilterKeyboard.tsx` exactly as stated to clone the styles and remove the wrapper padding. Once applied, set status to ✅ Completed."

## 4. Final Review
* **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to refresh. Verify: 1. The ugly white gap around the Output keys is completely gone. 2. The Output keys look 100% identical to the Transpose keys in border thickness and color. 3. The gray dimming effect still works on notes outside the filter slider range. Do not proceed until the user types 'VALIDATED'."