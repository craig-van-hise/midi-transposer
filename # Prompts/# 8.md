# Product Requirements Prompt (PRP) — EXECUTION TARGET OVERRIDE 🔘

## 1. Context & Objective
**STATUS: SEVERE EXECUTION FAILURE - CORRECTIVE ACTION**
You (the worker agent) previously failed to execute instructions because you edited a Markdown prompt file (`# 7.md`) instead of the actual codebase. You must immediately cease modifying ANY `.md` files. You are to apply exact UI geometry fixes to the React `.tsx` files to center the Output controls, fix clipping regressions, and compact the vertical space.

## 2. Mandatory Execution Constraints
* **DO NOT** edit, open, or read from the `# Prompts` directory.
* **DO NOT** edit any `.md` files. 
* All edits must be made exclusively to `src/App.tsx`, `src/components/keyboards/NoteRangeFilterKeyboard.tsx`, and `src/components/keyboards/KeySplitKeyboard.tsx`.

## 3. Implementation Phases

### Phase 6.1: Output Card (NoteFilter) Centering & Shadow Fix — [Status Marker] 🔘
* **Objective:** Center the filter controls, fix the missing drop shadow, and fix the clipped outline.
* **Instructions:**
  1. Open `src/components/keyboards/NoteRangeFilterKeyboard.tsx`.
  2. **Restore Shadow & Outline:** Locate the main keyboard wrapper `div` (the one containing `<Piano88Filter />`). Add `p-[2px]` to prevent border clipping, and add the missing shadow: `shadow-[0_4px_10px_rgba(0,0,0,0.2)]`. Ensure the white keys have dark borders (`#7a7a7a` or `border-neutral-700`).
  3. **Center the Controls:** Locate the top header area containing "FILTER MODE" and the toggle buttons. 
  4. Apply a flex layout that explicitly **centers** the items: `w-full flex justify-center items-center gap-6`. 
  5. The "FILTER MODE" text and the button group must sit comfortably in the center of the card, completely clear of the "Output" label in the top left.
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Edit the `.tsx` file exactly as stated. Once applied, set status to ✅ Completed."

### Phase 6.2: Input Card (KeySplit) Compacting — [Status Marker] 🔘
* **Objective:** Remove dead vertical space above the knobs.
* **Instructions:**
  1. Open `src/components/keyboards/KeySplitKeyboard.tsx`.
  2. Locate the root `div` (the card container). Change its top padding to `pt-4` (or similar minimal value) to pull the top of the card down closer to the Octave Knobs and Zones.
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Edit the `.tsx` file. Once applied, set status to ✅ Completed."

### Phase 6.3: App.tsx Margin Tightening — [Status Marker] 🔘
* **Objective:** Reduce the massive gaps between the cards on the canvas.
* **Instructions:**
  1. Open `src/App.tsx`.
  2. Locate the main flex container holding the three components.
  3. Change the gap to `gap-4` (16px).
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Edit `App.tsx`. Once applied, set status to ✅ Completed."

## 4. Final Review
* **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to refresh. Verify: 1. The Output card's filter controls are now perfectly CENTERED. 2. The Output keyboard has its shadow back and isn't clipping. 3. The cards are closer together vertically. Do not proceed until the user types 'VALIDATED'."