# Product Requirements Prompt (PRP) — UI POLISH & UNIFICATION 🔘

## 1. Context & Objective
**STATUS: LAYOUT & CONSISTENCY PASS**
The project architecture is correct. We must now refine the presentation: eliminate all dead space above the header, consolidate all three components onto the screen by reducing vertical gaps, and standardize the collapsible header style. The goal is a clean stack of three cards, each using the exposed Chevron with text model current found on `TransposeKeyboard88.tsx`.

## 2. Technical Decisions & Dependencies
* **Iconography:** Use `lucide-react` for standardizing the Chevron icon (`ChevronDown` and `ChevronUp`).

## 3. Implementation Phases

### Phase 3: Layout Tightening (`App.tsx`) — ✅ Completed
* **Objective:** Remove top dead space and drastically reduce vertical gaps.
* **Instructions:**
  1. Open `src/App.tsx`.
  2. Locate the main container `div` wrapping the entire page (inside the `return` block).
  3. **Fix Top Margin:** Remove any `pt-*` (top padding) classes from this main container to ensure the Header hugs the top of the viewport.
  4. **Tighten Gaps:** Locate the `div` holding the three keyboard components. Change its `gap-*` class from `gap-[120px]` to exactly `gap-3` (12px).
* **TDD Checkpoint:**
  * **Test Case 1:** [Given App renders, When viewport is inspected, Assert main wrapper has `padding-top: 0px` and keyboard container has `gap: 12px`].
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Execute these specific Tailwind class edits. Do not modify the components yet. Once applied, set status to ✅ Completed."

### Phase 4: Header Standardization — ✅ Completed
* **Objective:** Unify header styling using the "Exposed Chevron + Text" master model.
* **Instructions:**

  **STEP 1: Overwrite `KeySplitKeyboard.tsx` Header**
  1. Locate the top header flex container (currently has text "KEYSPLIT KEYBOARD").
  2. Nuke that internal content and replace it with the exact logic modeled from `TransposeKeyboard88`.
  3. Ensure the text reads "Input".
  4. Ensure the `isCollapsed` state is correctly wired to rotate the standard `ChevronDown` (or equivalent) icon and collapse the piano bed.

  **STEP 2: Review `TransposeKeyboard88.tsx` Header**
  1. Review the existing logic. It is current the master template. Text must continue to read "Transpose". Ensure it uses the exposed standard `ChevronDown` style (no button wrapper). *Self-correct: If it currently uses a button wrapper, extract the logic so the Chevron and text are directly inside the main flex header, styled with hover states.*

  **STEP 3: Overwrite `NoteRangeFilterKeyboard.tsx` Header**
  1. This component currently lacks a standard collapsible header.
  2. Implement the full collapsible header logic from `TransposeKeyboard88` into the root of `NoteRangeFilterKeyboard.tsx`.
  3. Wire a new local `isCollapsed` state hook.
  4. The text must read "Output".
  5. When collapsed, only this new header should be visible. When expanded, show the full original contents (the piano bed, range sliders, block mode dropdown).

* **TDD Checkpoint:**
  * **Test Case 1 (Input):** [Given `KeySplitKeyboard`, Assert header contains text "Input" and exposed Chevron].
  * **Test Case 2 (Input Collapse):** [Given `KeySplitKeyboard`, When header is clicked, Assert component `isCollapsed` state toggles and piano bed is hidden/shown].
  * **Test Case 3 (Output):** [Given `NoteRangeFilterKeyboard`, Assert header contains text "Output" and exposed Chevron].
  * **Test Case 4 (Output Collapse):** [Given `NoteRangeFilterKeyboard`, When header is clicked, Assert component `isCollapsed` state toggles].
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Modify the files to standardize the headers exactly as instructed. Once applied, set status to ✅ Completed."
* **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to refresh the page. Verify: 1. The title bar hugs the top. 2. Vertical gaps between cards are ~10px-12px. 3. All three cards use the exposed Chevron style and read 'Input', 'Transpose', 'Output' respectively. 4. Clicking the header of any card collapses/expands it correctly. Do not proceed until the user types 'VALIDATED'."