# Product Requirements Prompt (PRP) — GIT RECOVERY & REGRESSION FIX 🔘

## 1. Context & Objective
**STATUS: CRITICAL REGRESSION FIX**
The recent refactor of `KeySplitKeyboard.tsx` destroyed vital UI mechanics: independent edge resizing handles, Shift-click seam decoupling, and drag-and-drop zone reordering. 
**Objective:** Recover the original `KeySplitKeyboard.tsx` mechanics from the Git history. Adapt those recovered mechanics to support exactly two zones (Transpose and Play) without the legacy "Channels" drop-downs or "Add/Delete" zone features.

## 2. Mandatory Execution Constraints
* **DO NOT** write the drag-and-drop or resize logic from scratch. You must retrieve it from the local Git history.
* Target `.ts` and `.tsx` files ONLY.

## 3. Implementation Phases

### Phase 1: Store Architecture Reversion — ✅ Completed
* **Objective:** Replace `splitPoint` with a robust `zones` array to support decoupling and reordering.
* **Instructions:**
  1. Open `src/store/useMidiStore.ts`.
  2. Remove `splitPoint` from the state.
  3. Update the `Zone` interface to: `{ id: string; type: 'transpose' | 'play'; startNote: number; endNote: number; color: string; octave: number; }`
  4. Initialize the `zones` state with exactly two objects:
     * Transpose: `id: 'z-trans'`, `type: 'transpose'`, `startNote: 21`, `endNote: 59`, `color: '#f43f5e'`, `octave: 0`
     * Play: `id: 'z-play'`, `type: 'play'`, `startNote: 60`, `endNote: 108`, `color: '#3b82f6'`, `octave: 0`
  5. Add `setZones: (zones: Zone[]) => void` to the store and remove `setSplitPoint`.
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Update the store architecture. Once applied, set status to ✅ Completed."

### Phase 2: Git Recovery of KeySplitKeyboard — ✅ Completed
* **Objective:** Retrieve the complex mechanical code from the previous working commit.
* **Instructions:**
  1. Use your terminal/environment tools to read the `src/components/keyboards/KeySplitKeyboard.tsx` file from the previous commit (e.g., `git show HEAD~1:src/components/keyboards/KeySplitKeyboard.tsx` or however far back you need to go to find the version with active handles, shift-decoupling, and drag-and-drop reordering).
  2. Overwrite the current `KeySplitKeyboard.tsx` with this recovered code to restore the chassis.
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Recover and overwrite the file from Git. Once applied, set status to ✅ Completed."

### Phase 3: Adapt the Recovered Chassis — ✅ Completed
* **Objective:** Wire the recovered mechanics to the Transpose/Play logic.
* **Instructions:**
  1. Open the newly recovered `src/components/keyboards/KeySplitKeyboard.tsx`.
  2. Wire it to read and write the `zones` array from `useMidiStore`.
  3. **Strip Obsolete UI:** Remove the channel selection dropdowns from the zone headers. Remove any "Add Zone" buttons or double-click-to-delete logic. 
  4. **Inject Static Labels:** The zone header text should simply render "Transpose" or "Play" based on `zone.type`.
  5. Ensure that the original resizing handles (left/right edges), the Shift-drag seam decoupling, and the drag-and-drop reordering mechanics are left perfectly intact and update the global `zones` array on change.
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Strip the legacy UI and bind to the new store. Once applied, set status to ✅ Completed."

### Phase 4: Update the MIDI Routing Engine — ✅ Completed
* **Objective:** Fix `useWebMidi.ts` to route based on the decoupled `zones` array instead of a `splitPoint`.
* **Instructions:**
  1. Open `src/hooks/useWebMidi.ts`.
  2. Locate the logic block inside `handleMidiMessage`.
  3. Instead of checking `incomingNote < splitPoint`, retrieve the `zones` array from the store.
  4. Find if the `incomingNote` falls within any zone: `const activeZone = state.zones.find(z => incomingNote >= z.startNote && incomingNote <= z.endNote);`
  5. If no `activeZone` is found (it fell into a decoupled dead zone), `return` immediately (drop the note).
  6. If `activeZone.type === 'transpose'`, apply `state.transposeOctave`, update `setTransposeTarget`, and do NOT send to output.
  7. If `activeZone.type === 'play'`, apply `state.playOctave`, calculate the transposition delta, apply the filter limits, and send to output.
  8. Update the visual feedback color in this hook to match `activeZone.color`.
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Update the routing engine. Once applied, set status to ✅ Completed."

## 4. Final Review
* **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to refresh. Verify: 1. You can grab the outer edges of the zones to resize them. 2. You can hold Shift and drag the center handle to separate the zones. 3. You can click and drag the Transpose zone to swap places with the Play zone. 4. Hitting keys in dead space correctly drops the notes. Do not proceed until the user types 'VALIDATED'."