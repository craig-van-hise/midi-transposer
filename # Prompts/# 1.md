# Product Requirements Prompt (PRP) — HOTFIX 🚨

## 1. Context & Objective
**STATUS: EMERGENCY LAYOUT RECTIFICATION**
The Phase 5 assembly resulted in severe visual clipping. The `KeySplitKeyboard`, `TransposeKeyboard88`, and `NoteRangeFilterKeyboard` have fixed internal widths (e.g., 988px piano beds + padding = ~1020px). The parent wrappers in `App.tsx` and the individual card wrappers are currently crushing these components, causing horizontal squishing and vertical clipping of absolutely-positioned elements (octave knobs, tooltips, dropdowns).

## 2. Technical Instructions

### Phase 5.1: CSS & Container Rectification — ✅ Completed
* **Objective:** Remove all responsive constraints that are breaking the fixed-width UI, and restore the original component boundaries.
* **Instructions:**
  1. **App.tsx Main Wrapper:** Ensure the main application container (below the Header) is set to accommodate at least 1100px of width. Change its classes to `w-max mx-auto flex flex-col gap-8 pb-12` (or similar) instead of using standard responsive breakpoints like `container` or `max-w-7xl`. 
  2. **Remove Overflow Hiding:** Audit `App.tsx` and any wrapper `div`s you created around the keyboards. **Explicitly remove** any `overflow-hidden`, `overflow-x-auto`, or `overflow-y-auto` classes. The keyboards must have `overflow-visible` to allow their knobs and tooltips to break their bounding boxes.
  3. **Card Padding & Headers:** Ensure the labels ("INPUT (KEY SPLIT)", etc.) are placed *above* the actual component cards, not inside a container that forces the component to shrink. 
  4. **Component Integrity:** Do not modify the internal padding or width variables inside `KeySplitKeyboard.tsx`, `TransposeKeyboard88.tsx`, or `NoteRangeFilterKeyboard.tsx`. The problem is entirely in how you mounted them in `App.tsx`.
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Apply these layout fixes. Once applied, set status to ✅ Completed."
* **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to refresh the page and confirm that all three keyboards are fully visible, no knobs or shadows are clipping, and the widths match the original designs. Do not proceed to Phase 4 (The MIDI Pipeline Engine) until the user types 'VALIDATED'."