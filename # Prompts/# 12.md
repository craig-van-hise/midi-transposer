# Product Requirements Prompt (PRP) — DOM UNCHOKING & SHADOW RESTORATION 🔘

## 1. Context & Objective
**STATUS: SURGICAL DOM FIX**
The `NoteRangeFilterKeyboard` is suffering from a clipped drop shadow and a 1px white border rendering glitch. Code analysis reveals this is caused by an unnecessary `overflowX: 'auto'` wrapper choking the `<Piano88Filter>` component. The objective is to remove this wrapper so the component's structure perfectly mirrors the working `TransposeKeyboard88`.

## 2. Mandatory Execution Constraints
* **DO NOT** edit any `.md` files. 
* Target `.tsx` files ONLY.

## 3. Implementation Phases

### Phase 9.1: Eradicate the Overflow Wrapper — [Status Marker] 🔘
* **Objective:** Remove the DOM elements that are clipping the drop shadow.
* **Instructions:**
  1. Open `src/components/keyboards/NoteRangeFilterKeyboard.tsx`.
  2. Locate the `Piano88Filter` subcomponent (around line 105).
  3. You will see it returns this structure:
     ```jsx
     <div style={{ overflowX: 'auto', padding: '0px' }}>
       <div id="piano-container-88" ...>
     ```
  4. **DELETE** the outer `<div style={{ overflowX: 'auto', padding: '0px' }}>` and its closing `</div>` tag. 
  5. The component must now directly `return` the `<div id="piano-container-88" ...>` element as its root node.
  6. Scroll down to the main `NoteRangeFilterKeyboard` render block (around line 228).
  7. Locate where `<Piano88Filter minRange={range[0]} maxRange={range[1]} />` is mounted. Remove the `<div className="w-full">` wrapper around it. Just mount the `<Piano88Filter />` directly inside the `gap-1` flex container.

* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Delete the two specific wrapper `div`s exactly as instructed. Once applied, set status to ✅ Completed."

## 4. Final Review
* **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to refresh. Verify: 1. The Output keyboard now has its soft drop shadow back, matching the Transpose card perfectly. 2. The weird 1px white clipping border is gone. Do not proceed until the user types 'VALIDATED'."