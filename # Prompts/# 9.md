# Product Requirements Prompt (PRP) — EXACT ALIGNMENT & MARGIN POLISH 🔘

## 1. Context & Objective
**STATUS: FINAL VISUAL POLISH**
The cards are rendering correctly, but the canvas margins and internal spatial alignments need strict refinement. 
1. The margin above the first card must equal the gap between the subsequent cards.
2. The Output card (`NoteRangeFilterKeyboard`) is vertically too tall and must be compacted.
3. The "FILTER MODE" toggle buttons on the Output card must be decoupled from the text and centered explicitly over the C4 key (Middle C) to create a unified visual anchor with the Transpose card above it.

## 2. Technical Instructions

### Phase 7.1: Uniform Canvas Margins (`App.tsx`) — [Status Marker] ✅ Completed
* **Objective:** Equalize the gap between the Header and Card 1 with the gap between the cards.
* **Instructions:**
  1. Open `src/App.tsx`.
  2. Locate the main flex container holding the three keyboards. 
  3. Ensure it has a specific gap (e.g., `gap-6` or 24px).
  4. Ensure the `padding-top` or `margin-top` applied to this container exactly matches that gap value (e.g., `pt-6` or `mt-6`). There should be no irregular tight spacing below the top Header.
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Execute the margin synchronization in `App.tsx`. Once applied, set status to ✅ Completed."

### Phase 7.2: Output Card Compacting — [Status Marker] ✅ Completed
* **Objective:** Reduce the vertical height of the Output card so it matches the Input card.
* **Instructions:**
  1. Open `src/components/keyboards/NoteRangeFilterKeyboard.tsx`.
  2. Locate the header/control area containing the "Output" label, "FILTER MODE" text, and the buttons.
  3. Remove any excessive vertical padding (e.g., `py-4`, `mt-4`, `mb-4`) from this container. Use minimal padding (like `pt-2 pb-1`) to bring the controls closer to the top edge and the slider below.
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Remove the excess vertical padding. Once applied, set status to ✅ Completed."

### Phase 7.3: Absolute C4 Alignment for Filter Buttons — [Status Marker] ✅ Completed
* **Objective:** Center the toggle buttons strictly over the C4 key to align vertically with the Transpose '0' marker.
* **Instructions:**
  1. In `NoteRangeFilterKeyboard.tsx`, restructure the top control row into a `relative` container: `w-full relative flex items-center h-[40px]`.
  2. Keep the "Output" dropdown label on the far left.
  3. Place the "FILTER MODE" text next to it (e.g., `ml-12` or just as a standard flex item on the left side).
  4. **The Anchor Fix:** Extract the container holding the 4 toggle buttons (Block, Octave Wrap, Wrap, Limit). Apply absolute positioning to this container so its horizontal center sits exactly over C4. 
  5. *Architectural Math:* The piano bed is 988px wide. C4 is the 24th white key. The center of C4 is exactly `446.5px` from the left edge of the piano bed. 
  6. Apply these classes to the button wrapper: `absolute left-[446px] -translate-x-1/2`.
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Apply the absolute positioning to the button group. Once applied, set status to ✅ Completed."

## 3. Final Review
* **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to refresh. Verify: 1. The space below the main title bar equals the space between the cards. 2. The Output card is vertically compact. 3. The Filter Mode toggle buttons are perfectly aligned vertically with the '0' notch on the Transpose card (centered over C4). Do not proceed until the user types 'VALIDATED'."