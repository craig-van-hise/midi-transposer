# Product Requirements Prompt (PRP) — ABSOLUTE GEOMETRY LOCKDOWN 🔘

## 1. Context & Objective
**STATUS: UI COMPONENT COMPACTING & ALIGNMENT**
The three cards currently have inconsistent horizontal widths, vertical internal padding, and bottom-margin spacing. The goal is to enforce a perfectly uniform vertical stack using hardcoded, absolute Tailwind classes so all three cards share the exact same footprint.

## 2. Technical Instructions

### Phase 6.1: Unified Card Dimensions (`w-` and `pb-`) — [Status Marker] ✅
* **Objective:** Force all three components to share the exact same width and bottom ground-clearance.
* **Instructions:**
  1. **KeySplitKeyboard.tsx:** Locate the root `div`. Ensure it explicitly contains `w-[1020px]` and `pb-[16px]`.
  2. **TransposeKeyboard88.tsx:** Locate the root `div`. Ensure it explicitly contains `w-[1020px]` and `pb-[16px]`.
  3. **NoteRangeFilterKeyboard.tsx:** Locate the root `div`. Replace `w-fit` with `w-[1020px]`. Add `pb-[16px]`.

### Phase 6.2: Output Card (NoteFilter) Compacting — [Status Marker] ✅
* **Objective:** Shrink the filter mode controls, push them to the right, and tighten the slider gap.
* **Instructions:**
  1. Open `NoteRangeFilterKeyboard.tsx`.
  2. **Top Header Alignment:** Locate the `div` containing the "FILTER MODE" label and the toggle buttons. Apply `w-full flex justify-between items-center` to push the buttons to the far right.
  3. **Button Shrinking:** Locate the map function generating the 4 mode buttons. Change their padding and text size from `px-6 py-2 text-sm` to `px-3 py-1.5 text-xs`.
  4. **Slider/Keyboard Gap:** Locate the container holding the `<RangeSlider>` and `<Piano88Filter>`. Reduce the vertical gap between them (e.g., change `gap-4` to `gap-1` or remove unnecessary `mb-` classes) so the slider sits directly on top of the keys.
  5. **Top Padding Normalization:** Ensure the root `div` uses `pt-[36px]` so its top headroom matches the Transpose component.

### Phase 6.3: Input Card (KeySplit) Compacting — [Status Marker] ✅
* **Objective:** Remove the dead space above the split zones/octave knobs.
* **Instructions:**
  1. Open `KeySplitKeyboard.tsx`.
  2. The Octave Knobs and Zones currently have too much empty white space above them. Locate the root `div` and adjust its top padding to exactly `pt-[36px]` (matching the Transpose base standard) so the internal elements shift upward, reducing the card's overall height.

### Phase 6.4: App.tsx Vertical Gaps — [Status Marker] ✅
* **Objective:** Ensure the cards sit close together on the main canvas.
* **Instructions:**
  1. Open `App.tsx`.
  2. Locate the flex container holding the three keyboards.
  3. Set its gap to exactly `gap-8` (32px) to ensure the cards are compact but provide just enough room so the absolute-positioned knobs at the top of the cards do not clip into the card above them.

* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Apply all 4 phases exactly as written using the explicit Tailwind values provided. Once applied, set top-level status to ✅ Completed."
* **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to refresh. Verify: 1. All three cards are exactly the same width. 2. The bottom padding is identical across all three. 3. The Output Filter buttons are small and aligned right. Do not proceed until the user types 'VALIDATED'."