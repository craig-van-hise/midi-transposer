# Product Requirements Prompt (PRP) — BAREBONES APP.TSX OVERWRITE ✅ Completed

## 1. Context & Objective
**STATUS: EMERGENCY APP.TSX OVERWRITE**
The layout wrappers in `App.tsx` are completely botched. The components already possess their own native card styling. The objective is to strip `App.tsx` down to a bare flex-column that only handles vertical spacing and background color, with zero redundant wrappers around the components.

## 2. Technical Instructions

### Phase 5.5: Hardcode Barebones App.tsx — ✅ Completed
* **Objective:** Overwrite the rendering logic in `App.tsx` to stack the components directly.
* **Instructions:**
  1. Open `src/App.tsx`.
  2. Replace the entire `return` block of the `App` component with the exact JSX below. 
  3. **DO NOT** add any text, headers, or status badges. **DO NOT** wrap the components in extra `div`s with borders, backgrounds, or shadows.

```jsx
return (
  <div className="min-h-screen bg-slate-50 w-full overflow-x-hidden flex flex-col items-center pt-24 pb-32 gap-32">
    <Header />
    <KeySplitKeyboard />
    <TransposeKeyboard88 />
    <NoteRangeFilterKeyboard />
  </div>
);