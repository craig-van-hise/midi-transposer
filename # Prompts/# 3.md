
# Product Requirements Prompt (PRP) — ARCHITECTURE RESET 🔘

## 1. Context & Objective
**STATUS: EMERGENCY STRUCTURAL REBUILD**
The worker agent incorrectly merged three distinct keyboard components into a monolithic `App.tsx`, completely destroying the project's architecture and layout separation. The objective is to extract the original component code, establish the correct file tree, and cleanly import the isolated components into a minimal `App.tsx`.

## 2. Technical Instructions

### Phase 1: File Tree Restoration — ✅ Completed
* **Objective:** Create the missing directory structure and restore the components to their own isolated files.
* **Instructions:**
  1. Create the directory `src/components/keyboards/`.
  2. Create three new files in this directory: `KeySplitKeyboard.tsx`, `TransposeKeyboard88.tsx`, and `NoteRangeFilterKeyboard.tsx`.
  3. You must populate these three files using the **exact, original source code** the user provided at the start of the project. Do not alter their native Tailwind classes, do not strip their `bg-white` or `shadow` properties, and do not change their hardcoded widths.
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Create the directories, create the files, and paste the pristine original code into them. Once completed, set status to ✅ Completed."

### Phase 2: App.tsx Clean Rebuild — ✅ Completed
* **Objective:** Replace the bloated `App.tsx` with a clean, routing-only container.
* **Instructions:**
  1. Open `src/App.tsx`.
  2. Delete EVERYTHING in the file.
  3. Re-write `App.tsx` to explicitly `import` the three components from `src/components/keyboards/` and the `Header` from `src/components/`.
  4. Use the following exact `return` block. Do not wrap the imported components in ANY extra `div`s, cards, or text labels:

```jsx
return (
  <div className="min-h-screen bg-slate-50 w-full overflow-x-hidden flex flex-col items-center pt-8 pb-32 gap-[120px]">
    <div className="w-full bg-white shadow-sm">
      <Header />
    </div>
    
    <KeySplitKeyboard />
    <TransposeKeyboard88 />
    <NoteRangeFilterKeyboard />
  </div>
);

```

* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Delete the old `App.tsx`, add the imports, and apply the exact return block. Once applied, set status to ✅ Completed."
* **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to refresh the page. Verify the file tree now contains the `keyboards` folder, and the UI renders the three original, fully-styled components with massive vertical spacing. Do not proceed until the user types 'VALIDATED'."

```

